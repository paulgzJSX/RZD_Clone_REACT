import { FiSearch } from 'react-icons/fi'
import styled from 'styled-components'

const SearchIcon = () => {
    return (
        <SearchIconContainer />
    )
}

export default SearchIcon

const SearchIconContainer = styled(FiSearch)`
    font-size: 1.7rem;    
`

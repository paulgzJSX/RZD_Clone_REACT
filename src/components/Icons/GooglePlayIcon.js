import { FaGooglePlay } from 'react-icons/fa'
import styled from 'styled-components'

const GooglePlayIcon = () => {
    return (
        <GooglePlayIconContainer />
    )
}

export default GooglePlayIcon

const GooglePlayIconContainer = styled(FaGooglePlay)`
    font-size: 1.3rem;
    margin-right: .3rem;
`

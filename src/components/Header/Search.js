import SearchIcon from '../Icons/SearchIcon'
import { HeaderLink } from '../../styles/SharedStyles'

const Search = () => {
    return (
        <HeaderLink>
            <SearchIcon />
        </HeaderLink>
    )
}

export default Search